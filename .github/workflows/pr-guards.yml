name: PR guards

# Thin caller — the guard bodies live in the template source repo's
# reusable-pr-guards.yml (its ADR-0004); Dependabot bumps the SHA pin.
# Adopting the shared copy restores the issue-link job this repo's local
# fork had silently dropped (pst#104's open decision, resolved: restore).
on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
    branches: ["main"]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: pr-guards-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Job id `guards` is ruleset contract: required contexts are
  # `guards / single commit` etc. — renaming this id breaks the ruleset.
  guards:
    uses: carpet-stain/project-starter-template/.github/workflows/reusable-pr-guards.yml@c970349d6020f06e0769e901879c3ba70b3243f4 # main
