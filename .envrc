# shellcheck shell=bash
source_env_if_exists .envrc.local

# gh's keyring holds the routine dev PAT as the default account (#151) —
# derive the env copies from it: the tofu github provider and git-cliff
# read GITHUB_TOKEN, and GH_TOKEN keeps gh itself pinned to the same
# token. No token literal lives in any repo file.
if GH_TOKEN="$(gh auth token 2>/dev/null)" && [[ -n $GH_TOKEN ]]; then
  export GH_TOKEN GITHUB_TOKEN="$GH_TOKEN"
fi

# tenv installs the OpenTofu version required_version pins, on demand — the
# uv/GOTOOLCHAIN-style flow, scoped to this repo.
export TENV_AUTO_INSTALL=true

# The state passphrase and R2 backend credentials are deliberately NOT
# exported here anymore (#59, ADR-0009): they were the one crown-jewel set
# direnv broadcast to every shell that entered this repo, agent shells
# included (dotfiles#160). They now live only in /infra/* SSM parameters
# (#126, ADR-0010) and are fetched at invocation by
# scripts/with-infra-secrets.sh, which the `just tofu` / `tofu-apply`
# recipes wrap around — gated behind a macOS Keychain prompt. So run tofu
# through `just`, not bare. What stays ambient is non-elevated only: the
# routine dev PAT (derived from gh's keyring above) and the Cloudflare
# identifiers (.envrc.local).
